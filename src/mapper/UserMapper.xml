<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="mapper.UserMapper">
    <sql id="sql">
        select * from user
    </sql>
    <select id="login" parameterType="user" resultType="User">
        <include refid="sql"/> where loginName=#{loginName} and password=#{password}
    </select>

    <select id="getUserList"  resultType="User" parameterType="hashmap">
        SELECT u.id,u.loginName,u.email,u.gender,s.stautsName,l.levelName,u.endLoginTime FROM USER u LEFT JOIN `level` l ON  u.level=l.id INNER JOIN stauts s ON u.stauts=s.id
        <!--<where>-->
            <!--<if test="loginName != null and loginName != ''">-->
                <!--and loginName like '%${loginName}%'-->
            <!--</if>-->
        <!--</where>-->
        limit #{pagestart},#{size}
    </select>

    <select id="userCount" resultType="integer">
        select count(*) from user
    </select>

    <delete id="delete" parameterType="user">
        delete from user where id=#{id}
    </delete>
</mapper>